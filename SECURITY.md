# 安全策略

## 支持范围

安全修复以默认分支 `main` 的最新版本为准。历史提交和个人 Fork 不单独维护。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交。报告中请包含：

- 受影响版本或提交；
- 复现步骤与最小示例；
- 可能影响；
- 已知缓解措施。

请勿在公开 Issue 中粘贴密钥、访问令牌、个人信息或尚未修复的利用细节。普通界面和数据错误可以直接提交公开 Issue。

## 项目安全边界

本项目默认不采集用户数据，不包含账号系统、服务端数据库或分析埋点。唯一的第三方交互是用户主动打开的百度百科链接及视频 iframe。生产 Worker 会添加内容类型嗅探防护、引用策略、权限策略和嵌入限制等响应头。

维护者会在发布前运行依赖审计、敏感信息扫描、资源检查和生产构建；但开源软件按许可证“现状”提供，使用者仍应在自己的部署环境中配置 HTTPS、访问控制和持续依赖更新。
