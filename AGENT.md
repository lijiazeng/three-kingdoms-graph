# AGENT.md

本文件适用于在本仓库中工作的自动化开发代理与贡献者。

## 基本原则

- 所有对话、说明、文档和代码注释使用简体中文。
- 修改范围保持聚焦；不要顺手重写无关模块或覆盖他人的未提交改动。
- 不得提交密钥、令牌、Cookie、个人路径、数据库、日志、构建缓存或本地配置。
- 不得引入电视剧剧照、演员照片、受限音视频或来源不明的第三方素材。
- 生成式媒体必须明确标注，并保留非官方、非人物冒充的边界。

## 数据约定

- 人物、事件、地点与关系的主数据位于 `app/sanguoData.ts`。
- 时间轴、专题导览和地图派生模型位于 `app/sanguoExplorerModel.ts`。
- 电视剧、原著、正史三类内容必须分开描述，不把文学情节直接写成史实。
- 地望不确定时必须标注考证状态，地图范围仅作关系示意。
- 新增人物、事件或地点时，应同步补齐图片、关联关系、三重叙事和测试断言。

## 代码约定

- TypeScript 保持严格类型，组件优先使用语义化 HTML 和可访问名称。
- 交互元素必须支持键盘焦点；动画应兼容 `prefers-reduced-motion`。
- 外部链接仅允许明确审核过的 HTTPS 地址，并使用 `noopener noreferrer`。
- 第三方 iframe 必须配置 `sandbox`、`referrerPolicy` 和最小化的 `allow` 权限。
- 不为当前纯浏览器项目增加账号、数据库、分析埋点或远程写入能力，除非需求明确授权。

## 媒体约定

- 人物、事件和地点必须一项对应一个独立媒体文件，不使用拼图裁切代替。
- 展示图优先使用 WebP；透明 Logo 与剪影可以保留 PNG。
- 人声默认使用单声道、24 kHz、48 kbps MP3；氛围音乐应控制文件体积。
- 重新生成媒体后必须验证文件数量、路径、可解码性和页面显示效果。
- 语音稿位于 `content/narrations/`，不得只朗读页面摘要，应提供有史料边界的完整介绍。

## 完成检查

提交前至少运行：

```bash
npm run lint
npm test
npm audit
```

同时检查 `git diff --check`、未跟踪文件、资源体积、外链域名和敏感信息扫描结果。
